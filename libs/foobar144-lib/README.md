# foobar144-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foobar144-lib` to build the library.

## Running unit tests

Run `nx test foobar144-lib` to execute the unit tests via [Jest](https://jestjs.io).
