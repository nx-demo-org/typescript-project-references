# foobar1-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foobar1-lib` to build the library.

## Running unit tests

Run `nx test foobar1-lib` to execute the unit tests via [Jest](https://jestjs.io).
