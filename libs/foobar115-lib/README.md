# foobar115-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foobar115-lib` to build the library.

## Running unit tests

Run `nx test foobar115-lib` to execute the unit tests via [Jest](https://jestjs.io).
