# subfeature72-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build subfeature72-lib` to build the library.

## Running unit tests

Run `nx test subfeature72-lib` to execute the unit tests via [Jest](https://jestjs.io).
