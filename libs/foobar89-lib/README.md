# foobar89-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foobar89-lib` to build the library.

## Running unit tests

Run `nx test foobar89-lib` to execute the unit tests via [Jest](https://jestjs.io).
