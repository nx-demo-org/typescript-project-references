# subfeature92-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build subfeature92-lib` to build the library.

## Running unit tests

Run `nx test subfeature92-lib` to execute the unit tests via [Jest](https://jestjs.io).
