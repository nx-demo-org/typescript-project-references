# foobar87-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foobar87-lib` to build the library.

## Running unit tests

Run `nx test foobar87-lib` to execute the unit tests via [Jest](https://jestjs.io).
